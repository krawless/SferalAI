# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# All REQUIRED variables are validated at startup. Missing or invalid values 
# will prevent the application from starting.
#
# Copy this file to .env and update with your actual values.
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (MySQL 5.7)
# -----------------------------------------------------------------------------
DB_HOST_PROD=localhost
DB_USER_PROD=root
DB_PASSWORD_PROD=your_password
DB_NAME_PROD=your_database
DB_PORT_PROD=3306

# REQUIRED: Prisma Database Connection String
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Validation: Must start with "mysql://" and be a valid connection string
DATABASE_URL="mysql://${DB_USER_PROD}:${DB_PASSWORD_PROD}@${DB_HOST_PROD}:${DB_PORT_PROD}/${DB_NAME_PROD}?charset=utf8mb4&collation=utf8mb4_general_ci"

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# REQUIRED: Application Environment
# Validation: Must be one of: development, production, test
NODE_ENV=development

# REQUIRED: Backend Server Port
# Validation: Must be a number between 1 and 65535
# Example: 3000, 3001, 8000
PORT=YOUR_BACKEND_PORT

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------

# REQUIRED: Frontend Development Server Port
# Validation: Must be a number between 1 and 65535
# Example: 5173, 3000, 8080
VITE_PORT=YOUR_FRONTEND_PORT

# REQUIRED: Backend API URL
# Validation: Must be a valid HTTP/HTTPS URL
# Important: Set this to match your backend PORT above
# Example: http://localhost:3000
VITE_API_URL=http://localhost:YOUR_BACKEND_PORT

